<div>
  <h4>Comment by <%= comment.author_name %></h4>
  <h5> <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</h5>
  <p class="comment"><%= comment.body %></p>
</div>
